# Browser Usage Rules

- **Do NOT use Google Chrome**: The user has reported frequent crashes with Chrome.
- **Preferred Browser**: Use Brave Browser for any automated testing or previews if supported by the environment.
- **Action**: If the automated browser tool (browser_subagent) is hardcoded to Chrome and continues to crash, prioritize manual verification via user-provided screenshots or design tool updates instead of attempting to open Chrome.
